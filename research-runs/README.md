# research-runs/

Local research artifacts created by `Playbook.Research`.

Each run is a unique subdirectory containing `manifest.json`, metrics, and summaries.
Directories are never overwritten.

This folder is gitignored except for this README.
